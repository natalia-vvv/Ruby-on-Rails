<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <strong>
    <%= form.label current_user.username %><br>
  </strong>
  <p>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
